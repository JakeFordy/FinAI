# Required: Anthropic API key for Claude AI features
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Liminal banking API base URL (defaults to https://api.liminal.cash)
LIMINAL_BASE_URL=https://api.liminal.cash

# Optional: Port to run the backend server on (defaults to 8080)
PORT=8080
